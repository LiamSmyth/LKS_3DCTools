<p><b>Cleanup Empty</b> — Removes empty layers left by mesh operations, then selects Layer 0.</p>
<p>Does not merge or flatten layers that still have content. For that, use Consolidate.</p>
<p><i>Non-destructive: only removes empty layers.</i></p>
