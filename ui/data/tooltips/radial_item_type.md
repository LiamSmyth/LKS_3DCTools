<p><b>Type</b> — How this node behaves in the pie:</p>
<ul>
<li><b>Action</b> — leaf that runs a command when released over it</li>
<li><b>Branch</b> — opens a child pie (no direct command)</li>
<li><b>List</b> — shows children in a side list panel instead of a ring</li>
</ul>
